# Taxi Mobile App Prototype

Simple front-end prototype that includes:

- Rider screen with a map.
- Real-time GPS location tracking using the browser geolocation watch API.
- **Request Ride** button.
- Driver screen listing incoming ride requests.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
